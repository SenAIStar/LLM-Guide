# 数据说明

`sample/` 里的内容都是合成的，只是让字段和代码流程有东西可读，不能直接拿去训练或回答真实法律问题。

真实数据建议分开放：

- `data/private/` 放不能提交到仓库的原始数据；
- `data/processed/` 放清洗和切分后的结果；
- `outputs/` 放 LoRA adapter、日志和评测输出。

每次生成数据版本时，记下来源、授权或许可证、下载时间、内容 hash、清洗版本和切分种子。原始文件不要直接覆盖，清洗规则发生变化时重新生成 `processed/`。
