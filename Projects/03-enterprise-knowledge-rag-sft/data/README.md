# 数据说明

`sample/` 只放合成样例，用来说明字段和处理流程。

- `documents.jsonl`：解析后的文档段落，包含版本和 ACL 元数据。
- `qa.jsonl`：问题、金标证据和可回答性标注。
- `sft.jsonl`：带检索上下文、引用和拒答目标的 ShareGPT 样本。

真实项目至少要做到：取得文档授权、删除不必要的个人信息、保留版本与有效期、按文档族切分训练/验证/测试集，并由业务人员复核金标答案。不要把私有企业文档提交到公开仓库或外部模型 API。
